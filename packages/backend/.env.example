# Database Configuration
# Prisma connection string - used by Prisma ORM
# This should match the PostgreSQL configuration from root .env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/casino_jackpot?schema=public"

# Application Configuration
# Application port - APP_PORT takes precedence over PORT (used in src/main.ts)
APP_PORT=3000
PORT=3000
